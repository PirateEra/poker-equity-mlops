apiVersion: v1
kind: Service
metadata:
  name: poker-api
  labels:
    app: poker-api
spec:
  type: ClusterIP
  selector:
    app: poker-api
  ports:
    - name: http
      port: 5001
      targetPort: http
      protocol: TCP
